
const initialState = {

    checking: true,
    // uid : null,
    // name : null

}



export const authRreducer = (state = initialState , action)=>{


    switch (action.type) {
        
    
        default:
            return state;
    }

}