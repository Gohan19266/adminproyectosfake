import { makeStyles } from '@material-ui/core/styles';
import Button from '@material-ui/core/Button';

export const useTransitionAlertsStyle = makeStyles((theme) => ({
    root: {
      width: '30%',
      position:'absolute',
      top:'90%',
      left:'70%'
    },
  }));